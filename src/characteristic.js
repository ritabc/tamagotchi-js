export class Characteristic {
  constructor(name, currentLevel, interval, increment) {
    this.name = name;
    this.currentLevel = currentLevel;
    this.interval = interval;
    this.increment = increment;
  }
}
